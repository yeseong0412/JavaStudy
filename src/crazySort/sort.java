package crazySort;

import java.util.List;

public class sort {
    public List<Integer> sort(int len, List<Integer> list){
        switch (len){
            case 1 -> {
                return list;
            }
            case 2 ->{
                if (list.get(0) > list.get(1)) :

            }
        }
    }
}
